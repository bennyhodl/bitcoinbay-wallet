/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/App';
import {name as BitcoinBayWallet} from './app.json';

AppRegistry.registerComponent(BitcoinBayWallet, () => App);
